import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mainContainer: {
    width: '100%',
    paddingVertical: 10,
    alignItems: 'center',
    overflow: 'hidden',
  },
});

export default styles;
