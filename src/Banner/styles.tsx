import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mainContainer: {
    alignItems: 'center',
    overflow: 'hidden',
    paddingVertical: 8,
    width: '100%',
  },
});

export default styles;
