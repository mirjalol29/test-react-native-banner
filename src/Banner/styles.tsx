import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mainContainer: {
    alignItems: 'center',
    overflow: 'hidden',
    width: '100%',
  },
  banner: {
    paddingVertical: 8,
  },
});

export default styles;
